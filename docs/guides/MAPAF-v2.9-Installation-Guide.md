# MAPAF v2.9 Installation Guide

1. Extract `MAPAF-Enterprise-v2.9.0-GA.zip`.
2. Run `installers/install.sh framework <target-directory>`.
3. Run `<target-directory>/mapaf-doctor`.
4. Open `dashboard/reports/doctor/doctor-overview.html` through a local HTTP server.
