# MAPAF v2.9 Known Issues

- Claude live generation requires external credentials and network access; governed replay remains supported.
- Dashboard HTTP checks require a local report server.
- iOS execution requires macOS, Xcode, and a booted simulator or connected device.
- Generated evidence is environment-specific and should be regenerated after `clean`.
