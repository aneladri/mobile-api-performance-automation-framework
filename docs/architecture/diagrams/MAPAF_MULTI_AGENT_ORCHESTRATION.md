# MAPAF Multi-Agent Orchestration

Work Item Agent -> Knowledge Agent -> Test Design Agent -> Impact Agent -> Pipeline Agent -> Release Advisor.

The orchestrator carries shared context, records handoffs, enforces approvals, applies failure policies, and publishes workflow evidence.
