# MAPAF Enterprise v2.5 - Mobile Enterprise

## Highlights
- Added `mobileEnterpriseDemo`
- Added RoomScan state-driven business workflow
- Added device diagnostics and session evidence
- Added capture-quality reporting
- Added healing telemetry in the enterprise summary
- Added configurable live-demo pacing
- Added Allure step attachments
- Added dashboard-ready mobile summary JSON
- Preserved `mobileFrameworkDemo` and `healingDemoTest`

## Known limitation
The current enterprise reference workflow uses `MockCaptureProvider`. Real camera capture remains controlled by the application and device hardware through `RealCaptureProvider`.
