# MAPAF Enterprise v2.7 Patch

This is an additive sprint patch for Allure Showcase and Presentation Mode.

Copy the files into the framework root and append the provided Gradle snippet.

Primary command:

```bash
./gradlew presentationDemo -PpresentationProfile=FULL
```
